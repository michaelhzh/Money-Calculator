{
    "name": "money-calculator",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "dev": "concurrently \"cd server && npm start server\" \"cd client && npm install && npm run start\"",
      "heroku-postbuild": "cd client && npm install && npm run build",
      "start": "cd server && npm install && node index.js",
      "server": "cd server && nodemon index.js"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
      "concurrently": "^6.2.0",
      "nodemon": "^2.0.7"
    }
  }